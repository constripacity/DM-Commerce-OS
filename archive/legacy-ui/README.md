# Legacy UI Snapshot

This folder stores the previous tab-based dashboard shell so designers and developers can reference patterns while the new DM Commerce UI ships. Nothing inside `archive/legacy-ui` is loaded at runtime; the folder is excluded from TypeScript compilation.
